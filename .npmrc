# Native modules (better-sqlite3, etc.) run inside Electron's utility process,
# so prebuilt binaries must match Electron's ABI rather than the system Node ABI.
# Keep `target` in sync with the installed Electron major version.
runtime=electron
target=28.3.3
disturl=https://electronjs.org/headers
